import styled from "styled-components/native";
import { colors } from "../../styles/colors";

export const Container = styled.View`
`;

export const SlideActive = styled.View`
    width: 20;
    height: 10;
    background-color:${colors.primary} ;
    border-radius: 20px;

`;